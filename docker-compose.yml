version: '3.8'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: carewise_backend
    ports:
      - "8000:8000"
    environment:
      - PROJECT_NAME=CareWise
      - API_V1_STR=/api/v1
      - ENVIRONMENT=production
      - BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:3000","http://127.0.0.1:5173"]
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: carewise_frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    restart: unless-stopped
